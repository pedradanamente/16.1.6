2) Faça um algoritmo que leia 2 valores A e B e apresente o maior valor.
(Suponha valores diferentes)

#################################################

algoritmo maiorValor
var
	inteiro A,B;
inicio
	escreva("Digite dois numeros:");
	leia(A,B);
	se (A>B) entao
		inicio
			escreva("O numero maior é:",A);
		fim
	senao
		inicio
			escreva("O numero maior é:",B);
		fim
fim
